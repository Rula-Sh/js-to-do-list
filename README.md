# To Do List 📝

A small JavaScript project that implements a to-do list where users can create, mark as complete, undo mark, and delete tasks using basic DOM manipulation.

## Key Concepts Used 🧩

### DOM Selection & Traversal

- `document.querySelector()`
- `document.querySelectorAll()`
- `parentElement`
- `firstChild, children.item()`

### Event Handling

- `addEventListener()`
- `e.key, e.keyCode`
- `e.target`
- `click()`

### DOM Manipulation

- `createElement()`
- `appendChild()`
- `innerHTML, innerText`
- `className`
- `classList.add() / classList.remove()`
- `remove()`

## Programming Languages Used 🛠️

- HTML
- CSS
- JavaScript

## Features 🌟

- Add tasks by typing and pressing Enter, or by clicking the + button.
- Mark tasks as completed and undo completion.
- Update an existing task or cancel the update.
- Delete tasks individually or clear all tasks.
- Show a pop-up warning if the user tries to add an empty task.

## Running the Project ▶️

1. Clone or download the repository.
2. Open index.html in any modern browser.

(No build tools or dependencies required).

## Screenshot 📸

![Screenshot of the To Do List interface](img/js-to-do-list-screenshot.png)
